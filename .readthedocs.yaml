version: 2

build:
  os: ubuntu-lts-latest
  tools:
    python: "3.13"

sphinx:
  builder: html
  configuration: docs/conf.py
  fail_on_warning: true

formats:
  - htmlzip

python:
  install:
    - method: pip
      extra_requirements:
        - docs
      path: .
